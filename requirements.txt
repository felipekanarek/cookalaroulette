# Dependências do scraper (Fase 2). O frontend NÃO depende de nada disto.
# Instale num venv:  python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9            # parser opcional para BeautifulSoup (o código também funciona sem)
playwright>=1.40     # navegador real p/ sites com JS/bloqueio (rode: playwright install chromium)
